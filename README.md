📦 QR Code Generator (Node.js CLI)
  This is a simple command-line QR Code Generator built using Node.js. It takes a URL as input from the user via the terminal and:

  ✅ Generates a QR code image (qr.png)

  📝 Saves the original URL into a text file (yourUrl.txt)

  Perfect for quick link sharing or encoding URLs as QR codes from the terminal!
